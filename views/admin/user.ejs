<!DOCTYPE html>
<html lang="ko">
<%- include('../include/include.ejs',{title:"관리자페이지"}) %>
<link rel="stylesheet" href="/static/css/adminUser.css">

<body>
    <%- include('../include/header.ejs') %>
    <div id="member-content" >
        <h2>회원 관리 페이지</h2>
        <button class="permissionBtn" onclick="userPermission()">권한대기 유저</button>
        <table id="member-table">
            <tr>
                <th>아이디</th>
                <th>이름</th>
                <th>닉네임</th>
                <th>이메일</th>
                <th>가입일</th>
                <th>수정일</th>
                <th>권한</th>
                <th>클래스</th>
                <th>휴대폰 번호</th>
            </tr>
            <% userList.forEach(user => { %>
                <tr>
                    <td><%= user.u_id %></td>
                    <td><%= user.name %></td>
                    <td><%= user.nk_name %></td>
                    <td><%= user.email %></td>
                    <td><%= user.createdAt %></td>
                    <td><%= user.updatedAt %></td>
                    <td><%= user.permission %></td>
                    <td><%= user.study_class %></td>
                    <td><%= user.phone %></td>
                </tr>
            <% }); %>
        </table>
    </div>
</body>
</html>