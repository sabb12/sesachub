<!DOCTYPE html>
<html lang="en">
    <%- include("../include/include", {title: "로그인 및 회원가입"})%>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/static/css/confirmation.css">
<body>
    <%-include("../include/header")%>
    <div class="bodyContainer">
      <%-include("../include/sideBar")%>
      <div class="section2Container">
        <div class="title">예약 관리</div>
        <div class="reservationContainer">
          <div class="reservationHeader">
            <div class="check_all">
              <input type="checkbox" name="checkbox" id="checkAll"/>
              <label for="checkAll"> 전체 선택 </label>
            </div>
            <div>No.</div>
            <div>예약 장소</div>
            <div>예약 시간</div>
            <div>예약 인원</div>
          </div>
          <div class="reservation_body">
            <% reservationData.forEach(reservation => { %>
              <div class="reservation_list_container">
                <div class="check_all">
                  <input type="checkbox" name="checkbox" class="check-option" data-r-id="<%= reservation.r_id %>" data-u-id="<%= reservation.u_id %>" />
                </div>
                <div><%= reservation.r_id %></div>
                <div><%= reservation.st_room %></div>
                <div><%= reservation.day %></div>
                <%if(reservation.time < 10){%>
                <div><%= `0${reservation.time}:00 ~ ${Number(reservation.time) + 1}:00` %></div>
                <%} else {%>
                <div><%= `${reservation.time}:00 ~ ${Number(reservation.time) + 1}:00` %></div>
                <%}%>
                <div><%= reservation.count %></div>
                <button class="deleteButton" onclick="deleteButton(this, '<%= reservation.r_id%>','<%= reservation.u_id%>')">삭제</button>
              </div>
              <% }); %>
            </div>
            <div class="reservationPagination">< 1,2,3,4,5 ></div>
          </div>
          <div class="reserCancelContainer">
            <button class="delete-all-btn" onclick="deleteAllCheckedBtn()">예약 취소</button>
          </div>
      </div>
    </div>
    <script src="/static/js/confirmation.js"></script>
  </body>
</html>